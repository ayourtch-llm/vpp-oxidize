[package]
name = "vpp"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Safe(ish) Rust wrappers for building VPP plugins"

[dependencies]
vpp-sys = { path = "../vpp-sys" }
